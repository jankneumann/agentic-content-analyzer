---
allowed-tools: Bash(git add:*), Bash(git pull:*), Bash(git commit:*), Bash(pr create:*), Bash(pr merge:*)
description: Steps to do after finishing a feature
---

Commit changes to current branch and then create a pull request. Merge pull request.

Update CLAUDE.md with lessons learned from implementing the feature and make sure that any plans used are stored in docs/plans.
