# Merge Log — 2026-07-20

---

## Session: 22:28 (claude)

### PRs Processed

| PR | Origin | Action | Rationale |
|----|--------|--------|-----------|
| #462 | dependabot | merged (squash) | fastapi `<0.129`→`<0.140`; all required checks + contract-test green. Verified safe: CI installs newest-allowed via `uv pip install` (not `--frozen`), so fastapi ≥0.129 (the "breaking" internals version the pin guarded) was actually exercised and passed. |
| #458 | dependabot | merged (squash, --admin) | paradedb `v0.24.1`→`v0.24.3` Dockerfile bump; required checks green, non-required `contract-test` transient failure (`FATAL: role "root" does not exist` container race) bypassed. |
| #460 | dependabot | merged (squash, --admin) | actions/setup-python 6→7; required checks green, same transient contract-test bypassed. |
| #459 | dependabot | merged (squash, --admin) | actions/setup-node 6→7; stayed MERGEABLE after #460 despite shared workflow files; required checks green. |
| #461 | dependabot | merged (squash) | starlette `<0.51`→`<1.4`. Conflicted with #462 on `pyproject.toml` after #462 landed; triggered `@dependabot rebase` to regenerate on current main AND re-run CI against the real landing combo (`fastapi<0.140 + starlette<1.4`), since the bump undoes an explicit safety pin. Rebased head went CLEAN (all required + contract-test green) → merged. |
| #432 | openspec (claude/*) | merged (squash, --admin) | `feat(cli): extend source curation to YouTube RSS`. 116 commits behind but `overlap_count: 0` (conflict-free). `refresh-branch` merged main in; fresh CI turned the stale `dependency-audit-python` failure green (stale-base, not a real defect) and all 6 required checks passed. Squash (not rebase) chosen to discard the refresh merge commit. |
| #431 | openspec (claude/*) | deferred (left open + triage comment) | 38-file YT-ingestion redesign + model-registry. 86 commits behind, 11 overlapping hot files. Main diverged (added `youtube_captions.py`/`youtube_keyframes.py`); this PR adds `youtube_routing.py`. Needs re-evaluation + substantial revision against current main before merge — not a mechanical rebase. |
| #457 | other | skipped (draft) | Draft; also the primary checkout's current branch (`agent/update-agent-skills`). |

### User Decisions
- Dependabot batch: "Merge all 5 now" — accepted merging the 3 infra bumps (#458/#460/#459) despite the transient non-required `contract-test` failure.
- #432: "Refresh + merge if green" — approved refresh-branch then merge on green required checks.
- #431: reevaluate against current main before any merge attempt; likely needs substantial revision/modification. Left open with a triage comment; no close.

### Post-Merge Cleanup
- No OpenSpec `openspec/changes/<id>/` directories among merged PRs (#432's diff was source + tests only) → no `/cleanup-feature` needed.

### Observations
- **`contract-test` is a non-required check and is flaky** (`FATAL: role "root" does not exist` during paradedb container startup) — it failed on 3 sibling dependabot PRs and passed on 2 others off the same base. Confirms the standing note that this check's failures are systemic/transient, not merge blockers.
- **CI ignores `uv.lock`** for test jobs (`uv pip install --system -e ".[dev]"`, not `uv sync --frozen`), so dependency-constraint bumps ARE exercised at their newest-allowed version — green CI on a constraint widening is meaningful. `security.yml` is the only job using `--frozen`.
- **`pyproject.toml` carries an explicit safety-pin comment** guarding fastapi≥0.129 / starlette≥0.51 router-internals changes. Both #462 and #461 raise those caps; handled #461 via rebase-then-fresh-CI so the actual landing combo was verified rather than force-merged.
- Two conflicting PR pairs surfaced and were sequenced correctly: #462↔#461 (both `pyproject.toml`) and #460↔#459 (both `security.yml`/`tauri-build.yml`).
- Environment quirks worked around: `main` checked out in a worktree (all merges via `gh`, server-side; merge-log committed via the worktree); `discover_prs.py`/`vendor_review.py` broken (absent `agent-coordinator` package) — discovered/classified via `gh` directly.
