# Merge Log — 2026-07-28

---

## Session: 02:15–13:30 (claude-code)

### PRs Processed

| PR | Origin | Action | Rationale |
|----|--------|--------|-----------|
| #474 | dependabot | merged (squash) | `actions/upload-artifact` 4→7. All checks green, zero file overlap with other PRs. Merged first per Step 10 ordering. |
| #475 | dependabot | merged (squash) | `fastapi` `<0.140`→`<0.141`. All checks green. |
| #476 | dependabot | merged (squash) | `playwright` 1.58→1.61. Required checks all green after `refresh-branch`. |
| #480 | openspec | merged (rebase) | `feat(gen-eval)`: CLI gen-eval coverage (ri-06), +19825/−63 across 70 files. All 6 required checks green; 17 clean structured commits, no `wip:` prefixes → rebase to preserve phase-by-phase history. |
| #477 | dependabot | **closed** | `chardet` `<6`→`<8` violates a deliberate, documented compatibility cap. Failed a **required** check. |

### Merge order and conflicting pairs

⚠ #475, #476, #477 and #480 all modify `pyproject.toml`. Ordering was chosen so the
largest `pyproject.toml`/`uv.lock` writer (#480) landed before the last dependency bump,
so #476 needed only **one** refresh rather than one per preceding merge.

Staleness was re-checked between every merge. #476 correctly flipped `fresh → stale`
(overlap: `pyproject.toml`) after #475 landed, and was resolved with `refresh-branch`
rather than `rerun-checks` — see below.

### Why #477 was closed rather than fixed

`pyproject.toml:155` pins `chardet>=5.2,<6` under the `crawl4ai` extra with the inline
rationale `# Requests 2.x rejects chardet 6+`. That cap is guarded by
`tests/config/test_cli_runtime_hygiene.py:21`:

```
AssertionError: assert <Version('6')> not in <SpecifierSet('<8,>=5.2')>
```

This failed on `test (rest)` — a **required** check. The guard did its job; the bump is
genuinely incompatible until `requests` itself supports chardet 6+. Dependabot will
reopen an equivalent PR if that changes upstream.

### CI failure classification (Step 5b)

Three distinct failure classes appeared, each handled differently:

| PR | Check | Class | Action |
|----|-------|-------|--------|
| #477 | `test (rest)` | **PR-specific** | Closed the PR — the failure is correct |
| #476 | `contract-test` | **latent main bug** | `refresh-branch` (branch was genuinely stale); came back CANCELLED |
| #476 | `Cold-start migration & boot` | **transient** | Merged as-is — `Docker pull failed with exit code 1` after retries, non-required |
| #480 | `contract-test` | **latent main bug** | Merged as-is — non-required, defect predates the PR |

**Explicitly did NOT use `rerun-checks` on the `contract-test` failures.** Rerun replays
the same merge commit, and since schemathesis draws random inputs, a rerun would simply
re-roll the dice until the latent defect was missed — hiding a real bug behind a green
check. See the Observations section.

### Branch protection context

Required contexts: `lint`, `secret-scan`, `test (cli-stack)`, `test (api)`,
`test (services-stack)`, `test (rest)`. `strict: false`, no required approvals.
`contract-test` and `Cold-start migration & boot` are **not** required — which is why
#476 and #480 were mergeable with them red. All four merged PRs had every required
check green.

Note: `scripts/merge_pr.py` blocks on *any* failing check and does not consult the
branch-protection required-contexts list (it consults protection only for approvals).
#476 and #480 were therefore merged via `gh pr merge` directly, which lets GitHub
enforce the true required set server-side. Worth considering as a skill improvement.

### Vendor Review Findings

None dispatched. #480 was the only vendor-review-eligible PR by size and origin; the
operator elected to merge directly, and the PR carried an unusually detailed
self-authored description.

### User Decisions

- **#480**: merge immediately rather than run merge-time validation
  (`/validate-feature --phase deploy,smoke,security,e2e`). All required checks were
  green and the only red check was a documented pre-existing main bug.
- **500 bug**: file a coordinator handoff describing the issue so an agent in this repo
  can pick it up — explicitly *not* fix it inside the merge pass.

### Blocked

- **Coordinator handoff could not be filed.** `write_handoff` and `register_session` both
  returned HTTP 403 `API key is not permitted to act as requested agent_id`. Cause:
  `AI_AGENT=claude-code_2-1-220_agent` is asserted as the `agent_id`, but
  `COORDINATION_API_KEY` maps to profile `claude_code_local`. `get_my_profile` succeeds
  because it asserts no identity; `write_handoff` *is* in the profile's allowed
  operations, so this is purely an identity-binding mismatch, not a permissions gap.
  Handoff content preserved verbatim pending a re-file.

### Post-Merge Cleanup

- #480 (`establish-cli-gen-eval-coverage`): remote branch deleted by GitHub; **local**
  branch deletion failed — `openspec/roadmap-workflow-surface-reliability` is held by the
  worktree at `.git-worktrees/roadmap-workflow-surface-reliability`. Left intact rather
  than force-removed, per the skill's operator-attention rule.
- Note: PR #480's branch is `openspec/roadmap-workflow-surface-reliability` (a roadmap
  branch) while its change-id is `establish-cli-gen-eval-coverage` — the branch carried
  ri-04/ri-05/ri-06 roadmap commits, so archiving decisions should account for more than
  the one change directory.

### Observations

- **The `contract-test` red is a real latent bug, not flakiness.** #476 changes exactly one
  line of `pyproject.toml` (a playwright dev-dependency pin) yet "caused" a 500 on
  `GET /api/v1/contents`; #480 "caused" one on `GET /api/v1/summaries`. Neither can. The
  schemathesis suite is finding a pre-existing defect ~50% of the time, which is why
  #474/#475 passed the identical check. Treating it as flake — the intuitive read, and the
  one the skill's Red Flags warn against — would have buried it.
- **The most prominent log lines were the wrong lead.** The Postgres logs are dominated by
  `invalid input value for enum contentsource` and `invalid input syntax for type inet`,
  but recon showed both are already handled: enum casts become 422 via the `DataError`
  handler (`error_handler.py:103`), and the `inet` failures come from audit middleware that
  swallows writer errors. The actual gap is that `ProgrammingError` has **no** handler and
  falls through to the bare-`Exception` 500 at `error_handler.py:278`. Volume of log noise
  is not evidence of relevance.
- **`Cold-start migration & boot` failed on a Docker registry pull**, not on code — the one
  genuinely transient failure of the session, and the only one where `rerun-checks` would
  have been the right tool. It was non-required and on a dev-dependency bump, so rerunning
  was not worth ~10 minutes of CI.
