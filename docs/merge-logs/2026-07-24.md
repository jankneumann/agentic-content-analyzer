# Merge log — 2026-07-24

---

## Session: 02:1x (claude / merge-pull-requests)

### PRs Processed

| PR | Origin | Action | Rationale |
|----|--------|--------|-----------|
| #473 | openspec | merged (rebase) | RI-05 real-ingestion CI tiers. Mechanically mergeable but had 6 valid unresolved Codex findings (4×P1, 2×P2); addressed all 6 + added a guard test before merging. |

### Active-Agent Guard
- Blocked at start by a **pinned, stale** registry entry `restore-railway-frontend-deployment` (heartbeat ~1 day old, PR #469 already merged, no live worktree). Investigated → confirmed stale pin → forced past with operator approval. Entry later cleared by post-merge GC.

### Vendor Review Findings (Codex, all confirmed real)
- #473 harness.py (P1): `claimed or claimed_from_run` fallback masked the empty-durable-result regression the tier exists to catch → fixed to only fall back when no result record exists.
- #473 test_scheduled_tier.py (P1): live eligibility suppressed the fixture half → always run fixtures now.
- #473 test_live_tier.py (P1): live tier hardcoded to 6 PR keys → parametrized from LIVE_ADAPTER_POLICIES (14 live-eligible ∩ fixture-backed).
- #473 real_ingest_evidence.py (P1): DB IntegrityError misclassified as adapter → broadened persistence signatures (+ guard test).
- #473 real_ingest_policy.py (P2): declared live max_attempts=2 unused → submit_live now applies it (was registry default 3).
- #473 pyproject.toml/ci.yml (P2): committing/real-Gmail suite leaked into the `rest` shard → added `not real_ingest` to the shared marker expression.

### User Decisions
- Investigate the blocking pin before forcing (chose "Investigate first", then approved force once confirmed stale).
- Address all 6 findings before merge (not merge-as-is or P1-only).
- Rebase-merge when CI green (openspec default).
- Approved post-merge OpenSpec cleanup (archive + worktree/branch removal).

### CI
- Fresh run on review-fix commit `bbd3cf27`: all required checks green, incl. `real-ingest-test (PR tier)` and `test (rest)`. Neon DB-branch jobs correctly skipped (no migration).

### Post-Merge Cleanup
- Archived change → `openspec/changes/archive/2026-07-25-real-ingestion-test-tiers-in-ci/`; promoted delta into new `openspec/specs/real-ingestion-ci/` (4 requirements). `openspec validate real-ingestion-ci --strict` ✓. Archive commit `04e2fc44` pushed to main.
- Decision-index regeneration: N/A (no `docs/decisions/` in this repo).
- Removed the feature worktree + local branch (`-D`, rebase-merged remnant — content provably on main).

### Observations
- **GC scope note:** the skill's Step 8.5 `worktree.py gc` ran repo-wide and removed 9 stale worktree directories (most already-merged features). It swept `roadmap-workflow-surface-reliability` (a pre-existing workspace with 17 unpushed commits) — its branch ref + commits were preserved; the worktree directory was **restored** afterward to return the environment to its prior state. No committed work lost.
- Pre-existing `openspec validate --all --strict` failures (8 specs: agent-db-integration, agentic-analysis, content-ingestion, content-references, graph-provider, openbao-secrets, specialist-tools, validate-api-contracts) are unrelated to this change.
