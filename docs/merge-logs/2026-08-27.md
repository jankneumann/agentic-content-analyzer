# Merge Log — 2026-08-27

---

## Session: 19:14 (claude-opus-5)

Final continuation of the 2026-08-25 triage pass. See [2026-08-25.md](2026-08-25.md) and
[2026-08-26.md](2026-08-26.md). #506 was the last PR left open from that pass.

### PRs Processed

| PR | Origin | Action | Rationale |
|----|--------|--------|-----------|
| #506 | openspec | merged (rebase, 28 commits) | Green after `refresh-branch`; archived and converged |

### Findings

**The green signal was against a stale base, so it was refreshed before merging.** #506's CI
was green but its merge base was 10 commits behind main. `pyproject.toml` and
`src/config/settings.py` were touched by both #506 and #491 (anydoc parser), which landed in
between. The hunks were independent on inspection — anydoc parser fields vs backup
`BACKUP_S3_*` fields; `anydoc` extra vs `boto3` — but `MERGEABLE/CLEAN` proves only textual
mergeability, and those 19 checks had never run against the combined tree. `refresh-branch`
moved head `5242f0fa` → `18a8ac61`, brought the base to 0 commits behind, and CI came back
19 pass / 0 fail. Merged on the fresh signal, not the stale one.

**The earlier `contract-test` timeout was confirmed transient.** Attempt 2 passed in 16m12s
against the same commit that was killed at the 30-minute ceiling on attempt 1 — consistent
with the runner-contention diagnosis (eight concurrent CI runs on 2026-08-25), not a
capacity regression in the PR.

**Archive was blocked by a delta-spec authoring defect, and it was a real one.**
`openspec archive` aborted cleanly (no files changed) with:

    cli-interface MODIFIED failed for header "### Requirement: Restore From Cloud Command" - not found

Root cause: `aca manage restore-from-cloud` **did** exist in code before this change
(`src/cli/manage_commands.py`, `src/cli/restore_commands.py` both present at `66172cad`), so
the author reasonably wrote `MODIFIED`. But OpenSpec's `MODIFIED` operates on *spec
documents*, not code — and the command had never been captured in
`openspec/specs/cli-interface/spec.md`. You cannot modify a requirement that was never
written down. Corrected by moving the requirement from `## MODIFIED Requirements` into the
existing `## ADDED Requirements` section: 2 requirements and 11 scenarios preserved
verbatim, only the section header changed. Archive then succeeded.

### User Decisions
- Refresh #506 first, then merge only if it stayed green.
- Pre-approve the post-merge OpenSpec cleanup rather than re-confirming after the merge.

### Post-Merge Cleanup
- `/cleanup-feature add-gx10-backup-scheme --post-merge --pr 506 --defer-commit` — **completed**.
- Archive: `openspec/changes/archive/2026-08-27-add-gx10-backup-scheme/` (24 files).
- Specs merged: `backup-and-restore` (created, +9), `cli-interface` (+2), `database-provider` (~1).
- Open tasks: none — all 112 checked, so no coordinator issues or follow-up proposal.
- `openspec validate --all --strict`: 65 passed, 8 failed. All three touched specs pass; the
  8 failures are untouched pre-existing specs missing a `## Purpose` section (repo-wide
  hygiene backlog, unrelated to this change).
- `--defer-commit` invariants verified: HEAD unchanged at `248a3066` when cleanup returned,
  nothing pushed, decision index regenerated *here* (re-run produced no drift) rather than
  deferred.

### Context Convergence
- Merged SHA: `248a306656ab8099d14b38312c86ecbc6ec17139` → convergence commit:
  `b4c3bc23213875f210670949bf8f1310a1bf9842` → pushed: `b4c3bc23213875f210670949bf8f1310a1bf9842`
- Refresh status: `degraded` (exit 2 — derived context only; no merge affected)
- Semantic index: `pending` for `b4c3bc23` — enqueued, never awaited
- Record: appended to `docs/merge-logs/context-convergence.jsonl`;
  operation `pcr-cc021001b5ab6c885dd9681e`; `Context-Refresh-Operation:` trailer present
- **Archive move and decision-index regen landed in the SAME commit** (24 archive files +
  4 `docs/decisions/` files of 32 total) — the binding the cleanup skill's 2026-05-12
  incident note exists to enforce
- Guards: active-agent clear (re-checked) · coordinator lock unavailable
  (`No module named 'coordination_bridge'`) → degraded to layers 1 and 3 · compare-and-swap ok

### Observations

- **`make decisions` does not exist either.** Yesterday's log recorded the missing
  `make architecture-refresh` / `make context-drift-gate` targets. Cleanup Step 6 requires
  `make decisions`, which is also absent from the Makefile's 99 targets. It was substituted
  with the implementation the skill names,
  `.claude/skills/explore-feature/scripts/archive_index.py --emit-decisions`, which worked.
  The architecture refresh has no such fallback and could not run at all. Three required
  make targets are missing; this is a single wiring gap at the `project-context-refresh`
  layer, and it is why all three convergence passes this week reported `degraded`.
- No `validate-decision-index` job exists in `.github/workflows/`, so the CI breakage the
  cleanup skill warns about is not currently armed here. The index was regenerated and bound
  to the archive commit anyway.
- **Pass complete.** All 9 PRs from the 2026-08-25 scan are resolved: 6 merged, 3 closed,
  0 open.

---

## Session: 23:41 (claude-opus-5)

### PRs Processed

| PR | Origin | Action | Rationale |
|----|--------|--------|-----------|
| #512 | other | merged (rebase, 8 commits) | Green after one lint fix; the follow-ups from this week's passes |

### Findings

**`lint` failed on a formatter version gap, not a code defect.** Local ruff 0.14.14 accepted
`src/config/settings.py`; CI pins 0.15.15, whose formatter reflows one over-long line — a
comment I had lengthened when repointing the phantom `docling[ocr]` extra. Fixed by running
the exact CI version via `uvx ruff@0.15.15` (1 file, 3-line reflow), then verifying
`check` and `format --check` clean across all 445 files under that version. `69b1cd7b`.

**First convergence pass with a working architecture refresh.** Every pass since
2026-08-25 warned `staged architecture refresh failed (exit 2)` because
`make architecture-refresh` did not exist and, once created, the Postgres analyzer failed
on a repository with no `.sql` migrations. #512 landed both fixes; this pass's producer
record reads `architecture -> fresh`, and `make context-drift-gate` reports `fresh` with
zero blocking drift. `refresh_status` is still `degraded` — from `openspec.projection`,
which the ri-10 spec classifies as informational and never blocking. That is the normal
steady state, not a regression.

### User Decisions
- Merge #512 if green (pre-authorized); the lint fix was applied without a second ask
  because it was a formatter reflow with no code change.

### Post-Merge Cleanup
- None eligible (not an OpenSpec change).

### Context Convergence
- Merged SHA: `c9002cb5` → convergence commit: `a9c80e765eed8ad0b35c473c189f9d260450f6ae`
  → pushed: `a9c80e76`
- Refresh status: `degraded` (exit 2 — `openspec.projection` informational only; all other
  producers `fresh`, including `architecture` for the first time)
- Semantic index: `pending` for `a9c80e76`
- Record: operation `pcr-c99db0066c63755f22bf7ac9`; trailer present
- Guards: active-agent clear · coordinator lock unavailable → layers 1 and 3 ·
  compare-and-swap ok

### Observations
- Upstream proposal opened for the gate redesign: `agentic-coding-tools#427`
  (`rescope-architecture-freshness-to-on-demand`), sibling of #426. The
  `refresh_architecture.sh` fix in #512 is a local edit to an installed skill and will be
  overwritten by the next installer run until #427's Phase 1 lands upstream.
- Pin the dev environment's ruff to CI's version, or make `uvx ruff@<ci-version>` the
  pre-push habit; this is the second formatter-only CI failure this week.
