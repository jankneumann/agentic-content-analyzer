# Merge Log — 2026-05-19

---

## Session: 07:30 (claude-opus-4-7 / explanatory)

### PRs Processed

| PR | Origin | Action | Rationale |
|----|--------|--------|-----------|
| #422 | other | merged (squash, --admin) | Fresh, current branch, all required checks passing; only `contract-test` was cancelled (not a required context). Vendor review flagged 1 disagreement (perplexity/xsearch test env-var probe) — not a hard blocker. |
| #420 | dependabot (docker) | merged (squash, --admin) | Fresh single-file paradedb v0.23.0→v0.23.4 bump. Stale-base CI fixed via `refresh-branch` before merge. |
| #417 | openspec | merged (rebase, --admin) | Small (2 files, 4 lines): operation_id pin on `/search` and `/graph/query`. Stale-base CI fixed via `refresh-branch`. No openspec change-id artifact — likely misclassified by discover script. |
| #413 | dependabot (gha) | merged (squash, --admin) | actions/setup-python 5→6 in `security.yml`. Merged BEFORE #414 because both touch `security.yml`. |
| #414 | dependabot (gha) | merged (squash, --admin) | actions/setup-node 4→6 in `security.yml`/`ios-build.yml`/`tauri-build.yml`. Merged AFTER #413 to keep ordering deterministic. |
| #416 | other | skipped | Vendor review found 1 **confirmed high-criticality** blocking finding: hybrid search now runs synchronous BM25 SQLAlchemy inside the async path *before* awaiting vector search — blocks event loop and serializes what used to be parallel via `asyncio.gather`. Needs a fix in `src/services/search.py` before merge. Also 29 commits behind with 5 overlapping files. |
| #410 | openspec | skipped | 2 unresolved codex review comments — P1 ("Add readwise to queued ingestion source map" in `src/cli/ingest_commands.py:1551`) and P2 ("Pass readwise options through backend ingest contract" in `src/cli/ingest_commands.py:1547`). Also stale (overlaps `ingest_commands.py`, `settings.py`, `orchestrator.py` with just-merged #422 changes — needs rebase). 0 vendor blocking findings but 8 unconfirmed quality nits. |

### Vendor Review Findings

- **#422** (8 findings, 0 confirmed, 7 unconfirmed, 1 disagreement, **1 blocking**)
  - Disagreement: codex flagged `tests/e2e/test_ingest_variants_remote.py` perplexity/xsearch variants lack `requires_env`/backend capability probe (would fail when API keys absent); gemini disposition was `accept`. Recommended disposition: escalate. Decision: merge — gemini's read aligned with the existing test skip semantics.
  - Other unconfirmed: LLMResponse `.content`→`.text` drift (already addressed in commit 28a869d on this branch), OAuth hydration duplication, `_railway_set_env` ARG_MAX risk, E2E_ADMIN_KEY conftest fix.
- **#416** (7 findings, 1 confirmed, 6 unconfirmed, **1 blocking**)
  - Confirmed high: hybrid search sync-BM25-in-async-path perf regression (both vendors). Match score 0.95.
  - Unconfirmed quality: missing `contracts/openapi/v1.yaml` referenced by MCP spec; pagination boundary instability on `ingested_at >= since`; fuzz coverage gaps on KB/Graph/Reference endpoints; CVE-2026-3219 suppression rationale.
- **#410** (8 findings, 0 confirmed, 0 blocking)
  - All unconfirmed quality: N+1 highlight SELECTs, no upper bound on Retry-After sleep, COMPLETED status not reset on Readwise book update, ALTER TYPE ADD VALUE in transaction risk on old PG, `Any` instead of `Session` type hint, multi-line note blockquote wrapping.

### User Decisions

- Unpinned worktree registry entry `use-paradedb-railway-langfuse-default` (heartbeat from 2026-04-11, ~38 days stale, presumed forgotten pin).
- "Execute full plan" — authorized merging all 5 recommended PRs and skipping #416/#410.

### Post-Merge Cleanup

- No OpenSpec cleanup needed — #417 was classified openspec by `discover_prs.py` but has no `openspec/changes/<change-id>/` artifact, so the `post_merge_cleanup.py` filter excluded it correctly.

### Observations

- **Branch protection ⇄ matrix test name drift**: required contexts on `main` include literal `test`, but the actual matrix produces `test (api)`, `test (cli-stack)`, `test (services-stack)`, `test (rest)`. Every PR reads `state: BLOCKED` until either the protection rule is updated to enumerate matrix children OR a wrapper job named `test` is restored. `--admin` is currently the only path to merge. Worth fixing in a follow-up to remove the need for routine admin overrides.
- **Stale-base CI pattern**: 5 of 7 open PRs had identical `test`/`contract-test`/`Cold-start migration & boot` failures because they were branched off old main (29 commits behind). `refresh-branch` resolved this cleanly across all 4 dependabot/openspec PRs that needed it — the right tool per skill Step 5b.
- **Hotel-wifi → hotspot connectivity flip mid-session**: hotel wifi blocked ICMP entirely and selectively shaped HTTPS to github.com (browser-OK / CLI-timeout pattern, no system proxy involved). Cell hotspot restored connectivity. The watcher script absorbed an 11-min mid-session network gap and recovered automatically — useful pattern for flaky-network triage sessions.
- **Local skill patches (uncommitted)**: bumped `GH_TIMEOUT`/`GIT_TIMEOUT` from 30s/60s to 180s in `.claude/skills/merge-pull-requests/scripts/_helpers.py`, and swapped the `gh auth status` auth probe for `gh api user --jq .login` (the former trips on slow networks because of its network-side token validation). Worth keeping as a follow-up commit — these are genuine robustness improvements, not session-specific hacks.
- **Vendor review eligibility**: 4 of 7 PRs were eligible by origin; 1 (#417) auto-skipped as "small" (4 lines, 2 files); 3 reviewed (#422, #416, #410). Each review took ~5–10 min for codex+gemini in parallel. Net: 1 of 3 reviews surfaced a confirmed blocking finding (#416 perf regression) — a hit rate that justifies the spend.
