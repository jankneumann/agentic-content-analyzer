# Merge Log — 2026-08-26

---

## Session: 11:21 (claude-opus-5)

Continuation of the 2026-08-25 triage pass. See [2026-08-25.md](2026-08-25.md) for the
originating session; #491 and #506 were left open there with fixes pushed and CI running.

### PRs Processed

| PR | Origin | Action | Rationale |
|----|--------|--------|-----------|
| #491 | openspec | merged (rebase) | Fully green (15 pass, 0 fail); `MERGEABLE/CLEAN`; re-verified before merge after the overnight gap |
| #506 | openspec | still open | 18 pass, `contract-test` re-run in progress (attempt 2) |

### Findings

**`rerun-checks` silently re-ran the wrong runs.** A job killed by `timeout-minutes`
reports `conclusion=cancelled`, **not** `failure`. `merge_pr.py rerun-checks` filters for
*failed* workflow runs, so it skipped the actually-broken run (`32851165845`, head
`5242f0fa`) and instead re-ran a stale historical run on head `b5bcdf3c` — an old commit on
the same branch. It returned `"rerun": true` for work irrelevant to the PR's current state,
and no new run appeared for the current head.

Verified by comparing the returned run IDs against `gh run list` for the branch, then
re-running the correct run directly (`gh run rerun 32851165845 --failed` → attempt 2).
**Worth fixing in the skill:** the failed-run filter should also match `cancelled`, which is
how every `timeout-minutes` kill surfaces.

**#506's contract-test timeout was contention, not the PR.** Same PR's contract tests passed
in 13m25s on `3a1c6651` (2026-08-21); the run on `5242f0fa` stalled at 39% and was killed at
the 30-minute ceiling. The intervening commit changed only comments and whitespace. Eight CI
runs fired between 12:58 and 13:09 on 2026-08-25 (four merges plus two branch pushes),
competing for runners on CPU-heavy schemathesis fuzz tests. Classified transient per Step 5b
→ `rerun-checks`, not `refresh-branch` (the merge base was already current).

### User Decisions
- Merge #491 once confirmed green; leave #506 pending its re-run.

### Post-Merge Cleanup
- None eligible: #491 has no local `openspec/changes/` directory
  (`post_merge_cleanup.py` → "No merged local OpenSpec PRs need post-merge cleanup").

### Context Convergence
- Merged SHA: `bb76e87db91f25dffefd6d6b92714c8de6dd98c9` → convergence commit:
  `f24199724e0b5b86300d1533943a93d49d7ae806` → pushed: `f24199724e0b5b86300d1533943a93d49d7ae806`
- Refresh status: `degraded` (exit 2 — derived context only; no merge was affected)
- Semantic index: `pending` for `f2419972` — enqueued, never awaited
- Record: appended to `docs/merge-logs/context-convergence.jsonl` (3 records total);
  operation `pcr-469bbf85c0be456d05df91e7`; `Context-Refresh-Operation:` trailer present
- Guards: active-agent clear (re-checked) · coordinator lock **unavailable**
  (`No module named 'coordination_bridge'`) → degraded to layers 1 and 3 · compare-and-swap ok

### Observations

- The `make architecture-refresh` / `make context-drift-gate` gap logged on 2026-08-25 is
  **still open** and produced the identical three warnings on this pass, as predicted. Every
  convergence will report `degraded` until those targets are wired up at the
  `project-context-refresh` layer.
- Two skill-level defects surfaced across these two sessions, both in the same family —
  **a helper reporting success for work it did not do**: `rerun-checks` re-running stale runs
  (above), and the convergence driver reporting `drift` for a Makefile target that does not
  exist. Both are only visible if you verify the helper's claim against primary state rather
  than trusting its return value.
