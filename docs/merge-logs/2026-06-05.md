# Merge Log — 2026-06-05

---

## Session: 15:30 (claude / merge-pull-requests)

### PRs Processed

| PR | Origin | Action | Rationale |
|----|--------|--------|-----------|
| #426 | other (created this session) | merged (squash) | Fix for `main`'s broken `lint`; created to unblock #424/#425 |
| #425 | dependabot | merged (squash) | gitleaks-action 2→3; green after main lint fix + branch refresh |
| #424 | dependabot | merged (squash) | paradedb v0.23.4→v0.23.5-pg17; green after main lint fix + refresh |
| #423 | openspec (own) | merged (merge commit) | `aca deploy sync-secrets`; resolved blogs.yaml conflict + fixed stale deploy test |
| #416 | other | closed (obsolete) | Spec sync already on main; 40 commits behind; unresolvable auto-merge |

### Root-Cause: `main` lint was red (blocked everything)

- `pyproject.toml` has `[tool.ruff.lint] preview = true` + `select = ["RUF", ...]`, so preview rules are active.
- CI installed ruff **unpinned** (`uv pip install --system ruff mypy`); it drifted to `ruff==0.15.15`, which introduced preview rule **RUF075** (`fallible-context-manager`).
- RUF075 flagged a real bug: `src/api/app.py` lifespan `yield` was not wrapped in `try/finally`, so shutdown (worker cancel, telemetry flush, queue close) would be skipped on an exception during run.
- Fix (#426): wrap `yield` in `try/finally` + pin `ruff==0.15.15` / `mypy==2.1.0` in CI. This is what unblocked both dependabot PRs (their merge base was already current main, so the lint failure was inherited, not theirs).

### PR #423 — conflict + latent test failure

- Only conflict on refresh was `sources.d/blogs.yaml` (origin's `star.sh` commit vs the PR's scraper additions). Resolved as a **union**: took the PR's superset file (`--ours`) and grafted in main's single Stax.sh entry. Result: 13 entries, valid YAML, no scraper lost.
- After refresh, `test (rest)` failed — a **pre-existing latent failure** never caught because the PR was CONFLICTING since creation (CI never ran). `tests/test_cli/test_auth_commands.py` (from merged #422) asserted `gmail --deploy` makes exactly 1 railway call, but the `085058c4` shared-Railway-helper refactor added a `railway status --json` warning call (via `_warn_deploy_target`). Fixed both deploy tests to filter to `variables --set` calls, preserving their intent. Verified: full rest shard 1951 passed.

### PR #416 — re-evaluation (deep-diff requested)

- Branch refresh failed (HTTP 422, real conflicts in 6 files after 40 commits behind).
- Deep-diff (main vs #416) of all 5 specs: main already covers the work. knowledge-base — main's `KB API Endpoints` + `KB Health Checks` requirements cover #416's search/lint additions; content-references already normalized; audit-log/knowledge-graph/mcp-http-client deltas were 1–4 lines.
- Only unique-to-#416 framing: KB lint as HTTP endpoints (vs `aca kb lint` CLI) and a 3σ score-anomaly definition — re-derivable, captured in the close comment.

### User Decisions
- Local `main` was diverged (8 commits belonging to #423's branch); user chose to **triage directly against `origin/main`** rather than reset local main. All work done against `origin/main`; local main left untouched.
- Fix main lint via **PR + self-merge** (not direct push / admin-bypass).
- #423: **refresh + resolve conflict** (then merge).
- #416: **refresh & re-evaluate** → then **deep-diff specs** → then **close as obsolete**.

### Observations
- **Conflicting state masks CI**: #423 ran zero checks for its entire life because it was CONFLICTING from creation. Resolving the conflict surfaced a real test failure that had been invisible. Lesson: a long-lived conflicting PR is also an untested PR.
- **Unpinned linters are a latent main-breaker**: a preview rule landing in a fresh ruff release turned `main` red with no code change. Now pinned.
- **Shard routing quirk**: `tests/test_cli/` (underscore) routes to the `rest` shard, not `cli-stack` (which ignores `tests/cli/`). Easy to misattribute a failure's shard.
- Non-required checks `contract-test` (flaky/systemic) and `dependency-audit-python` (fails identically on unrelated PRs → pre-existing) are not merge blockers; merged via required-check gate.

### Post-Merge Cleanup
- #423 carries OpenSpec change `add-railway-secrets-sync` (now on main). Post-merge `/cleanup-feature add-railway-secrets-sync --post-merge --pr 423` offered to the operator (archive proposal + sync specs); not auto-run.
