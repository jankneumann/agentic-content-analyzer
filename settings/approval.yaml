risk_levels:
  search_content: low
  query_knowledge_graph: low
  analyze_themes: low
  get_historical_context: low
  ingest_source: medium
  ingest_url: medium
  run_pipeline: medium
  summarize_content: medium
  create_digest: high
  send_email: high
  publish_digest: high
  store_graph_episode: high
  delete_content: critical
  modify_agent_config: critical
  bulk_operations: critical
  # Model-registry freshness (auto-update-model-registry)
  model_pricing_update: low      # update cost/specs of existing models (auto-applies)
  model_registry_add: medium     # add a newly-discovered model to the registry
  model_default_swap: high       # change the model used for a pipeline step
