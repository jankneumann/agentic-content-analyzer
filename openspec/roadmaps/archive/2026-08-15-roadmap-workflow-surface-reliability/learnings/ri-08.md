---
schema_version: 1
item_id: ri-08
timestamp: '2026-08-02T00:26:04+00:00'
decisions:
- title: Persist exact attempt ownership
  outcome: Content and Summary state now carries operation and claim-generation provenance,
    and guarded domain commits fail closed after cancellation, reclaim, or displacement.
- title: Keep reconciliation bounded and default-off
  outcome: One-page dry-run/apply controls reuse canonical retry, enforce retry ceilings,
    and emit closed reports plus atomic append-only action evidence.
- title: Reuse the measured status index
  outcome: The 10,001-row plan scanned one candidate through the existing status index,
    so no redundant ownership index was added.
change_id: stuck-content-sweeper-and-requeue-cli
phase: validation
blockers:
- description: SQLAlchemy could return cached Content ownership after the row lock was acquired.
  resolution: Locked reads now populate existing instances, and displacement-during-compute
    regressions prove stale attempts cannot commit.
- description: Full-suite import order could initialize the embedded API worker before API-local
    test configuration disabled it.
  resolution: Root test initialization disables the embedded worker before any test module can
    import the application.
deviations:
- from_plan: Independent review was expected to validate the initial content lock implementation.
  actual: Review found and drove a High-severity identity-map refresh fix.
  reason: Database row locking alone does not refresh an already-loaded SQLAlchemy identity.
recommendations:
- Use RI-08 terminal outcomes and append-only action evidence as inputs to RI-09 telemetry and alerts.
- Resolve provider-error redaction and per-item apply-failure logging in GitHub issue #484.
---

# Learning: ri-08

## Decisions
- **Persist exact attempt ownership**: Content and Summary state now carries operation and claim-generation provenance, and guarded domain commits fail closed after cancellation, reclaim, or displacement.
- **Keep reconciliation bounded and default-off**: One-page dry-run/apply controls reuse canonical retry, enforce retry ceilings, and emit closed reports plus atomic append-only action evidence.
- **Reuse the measured status index**: The 10,001-row plan scanned one candidate through the existing status index, so no redundant ownership index was added.

## Blockers
- SQLAlchemy could return cached Content ownership after the row lock was acquired. → Locked reads now populate existing instances, and displacement-during-compute regressions prove stale attempts cannot commit.
- Full-suite import order could initialize the embedded API worker before API-local test configuration disabled it. → Root test initialization disables the embedded worker before any test module can import the application.

## Deviations
- Plan: Independent review was expected to validate the initial content lock implementation. → Actual: Review found and drove a High-severity identity-map refresh fix. (reason: Database row locking alone does not refresh an already-loaded SQLAlchemy identity.)

## Recommendations
- Use RI-08 terminal outcomes and append-only action evidence as inputs to RI-09 telemetry and alerts.
- Resolve provider-error redaction and per-item apply-failure logging in GitHub issue #484.
