---
schema_version: 1
item_id: ri-06
timestamp: '2026-09-26T11:07:33.803697+00:00'
decisions:
- title: Durable last_verified_at from ingestion history
  outcome: completed_at of the latest success or zero_items run of the gated source
    (substack, x_bookmarks) via OperationService.list_ingestion_history; remote profiles
    use GET /api/v1/ingestions
- title: Status never fails on lookup errors
  outcome: 5s cap, never raises, rows show unknown and stderr carries the exception
    type only
- title: Value-free JSON
  outcome: aca auth status --json emits oauth, browser_sessions, last_verified_lookup;
    Railway variables reported as booleans only
change_id: add-browser-session-rows-to-aca-auth-status
phase: implementation
deviations:
- from_plan: new browser-session-credentials capability for the spec
  actual: existing cli-interface capability
  reason: the change is a CLI surface
recommendations:
- 'ri-12: use command_key x_bookmarks exactly; BROWSER_SESSIONS in src/cli/browser_session_status.py
  looks it up'
- 'ri-08: keep the command names aca auth session substack|x, which the status rows
  hard-code'
- 'ri-16/ri-17: reuse build_session_rows() for alert text and docs'
---

# Learning: ri-06

## Decisions
- **Durable last_verified_at from ingestion history**: completed_at of the latest success or zero_items run of the gated source (substack, x_bookmarks) via OperationService.list_ingestion_history; remote profiles use GET /api/v1/ingestions
- **Status never fails on lookup errors**: 5s cap, never raises, rows show unknown and stderr carries the exception type only
- **Value-free JSON**: aca auth status --json emits oauth, browser_sessions, last_verified_lookup; Railway variables reported as booleans only

## Deviations
- Plan: new browser-session-credentials capability for the spec → Actual: existing cli-interface capability (reason: the change is a CLI surface)

## Recommendations
- ri-12: use command_key x_bookmarks exactly; BROWSER_SESSIONS in src/cli/browser_session_status.py looks it up
- ri-08: keep the command names aca auth session substack|x, which the status rows hard-code
- ri-16/ri-17: reuse build_session_rows() for alert text and docs
