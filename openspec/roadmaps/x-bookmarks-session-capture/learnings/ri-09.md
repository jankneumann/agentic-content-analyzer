---
schema_version: 1
item_id: ri-09
timestamp: '2026-09-26T11:07:33.811713+00:00'
decisions:
- title: XBookmarksIngestCommand contract
  outcome: strict, kind-only required; max_items 1..10000, full (default false), expand_links
    (no default, defers to source config), force_reprocess, internal configured_sources;
    no after_date since X does not expose bookmark time
- title: Enum and closed lists
  outcome: ContentSource.X_BOOKMARKS with migration d8e2a4c6f1b3 (ADD VALUE IF NOT
    EXISTS, idempotent); result literals ingest.x-bookmarks and x_bookmarks; MCP tool
    ingest_x_bookmarks; web display, filter, and contents route
- title: CLI generated from contract
  outcome: aca ingest x-bookmarks with --full, --expand-links/--no-expand-links, --max-items,
    --force-reprocess needs no code
change_id: add-x-bookmarks-contract-and-content-source
phase: implementation
deviations:
- from_plan: land independently
  actual: committed locally and pushed together with ri-10
  reason: test_capabilities_match_openapi_discriminator_and_fields requires the registry
    descriptor; an unregistered kind 500s
recommendations:
- 'ri-10: bulk planner maps max_entries to max_items and ignores after_date; readiness
  uses the shared credentials_missing code from src/ingestion/credential_failures.py'
- 'ri-10: add x_bookmarks to the worker dispatch map and real_ingest_policy as credentialed
  on X_AUTH_TOKEN + X_CT0'
---

# Learning: ri-09

## Decisions
- **XBookmarksIngestCommand contract**: strict, kind-only required; max_items 1..10000, full (default false), expand_links (no default, defers to source config), force_reprocess, internal configured_sources; no after_date since X does not expose bookmark time
- **Enum and closed lists**: ContentSource.X_BOOKMARKS with migration d8e2a4c6f1b3 (ADD VALUE IF NOT EXISTS, idempotent); result literals ingest.x-bookmarks and x_bookmarks; MCP tool ingest_x_bookmarks; web display, filter, and contents route
- **CLI generated from contract**: aca ingest x-bookmarks with --full, --expand-links/--no-expand-links, --max-items, --force-reprocess needs no code

## Deviations
- Plan: land independently → Actual: committed locally and pushed together with ri-10 (reason: test_capabilities_match_openapi_discriminator_and_fields requires the registry descriptor; an unregistered kind 500s)

## Recommendations
- ri-10: bulk planner maps max_entries to max_items and ignores after_date; readiness uses the shared credentials_missing code from src/ingestion/credential_failures.py
- ri-10: add x_bookmarks to the worker dispatch map and real_ingest_policy as credentialed on X_AUTH_TOKEN + X_CT0
