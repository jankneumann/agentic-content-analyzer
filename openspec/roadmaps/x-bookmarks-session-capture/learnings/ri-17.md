---
schema_version: 1
item_id: ri-17
timestamp: '2026-09-26T11:07:33.826874+00:00'
decisions:
- title: User guide and setup coverage
  outcome: Browser Sessions and X Bookmarks sections in USER_GUIDE; capture, sinks,
    env/settings tables and gx-10 session roles in SETUP; manual DevTools and --session-cookie
    instructions removed
- title: Gotchas
  outcome: frozen settings vs live provider, KV v2 clobbering and hvac patch, public
    X bearer token allowlist, alert classifier needs result codes, ruff 0.15.15 RUF102;
    KV v2 and ruff pin also in CLAUDE.md Critical Gotchas
- title: Alert remediation docs
  outcome: credential codes and remediation_command documented in docs/DEVELOPMENT.md
    where terminal-event alerts live
change_id: document-x-bookmarks-and-the-credential-lifecycle
phase: implementation
deviations:
- from_plan: document aca ingest substack-sync
  actual: removed all references
  reason: the command no longer exists in the CLI; sync_substack_sources() has no
    caller
recommendations:
- Recheck the link-expansion docs (X_BOOKMARKS_MAX_EXPANDED_LINKS, warnings) once
  ri-13 lands
- Run make context-refresh after ri-13 so the drift gate goes green
- Decide whether to restore a Substack subscription-sync command or delete sync_substack_sources()
---

# Learning: ri-17

## Decisions
- **User guide and setup coverage**: Browser Sessions and X Bookmarks sections in USER_GUIDE; capture, sinks, env/settings tables and gx-10 session roles in SETUP; manual DevTools and --session-cookie instructions removed
- **Gotchas**: frozen settings vs live provider, KV v2 clobbering and hvac patch, public X bearer token allowlist, alert classifier needs result codes, ruff 0.15.15 RUF102; KV v2 and ruff pin also in CLAUDE.md Critical Gotchas
- **Alert remediation docs**: credential codes and remediation_command documented in docs/DEVELOPMENT.md where terminal-event alerts live

## Deviations
- Plan: document aca ingest substack-sync → Actual: removed all references (reason: the command no longer exists in the CLI; sync_substack_sources() has no caller)

## Recommendations
- Recheck the link-expansion docs (X_BOOKMARKS_MAX_EXPANDED_LINKS, warnings) once ri-13 lands
- Run make context-refresh after ri-13 so the drift gate goes green
- Decide whether to restore a Substack subscription-sync command or delete sync_substack_sources()
