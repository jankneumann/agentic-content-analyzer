---
schema_version: 1
item_id: ri-11
timestamp: '2026-09-26T11:07:33.820246+00:00'
decisions:
- title: XBookmarksClient.iter_pages
  outcome: newest-first pages by cursor-bottom, stop_when(page_ids) hook, page cap,
    x_bookmarks_page_delay_s pacing; WalkStopReason exhausted/stop_when/page_cap/rate_limited/caller_stopped
- title: Durable query ID cache
  outcome: settings_overrides key x_bookmarks.graphql_query_id; stale answer clears,
    rediscovers once, retries once; bundle requests never carry cookies
- title: ct0 write-back as a pair
  outcome: X_AUTH_TOKEN and X_CT0 in one quiet BaoSink.write() on the worker's authenticated
    client, then apply_local_write with the pinned saved_at; failure never fails the
    fetch
- title: Value-free XPost records
  outcome: note-tweet full text, outbound URLs without X self links, best mp4, UTC
    created_at, one level of quoted post
change_id: implement-x-graphql-client-with-ct0-write-back
phase: implementation
deviations:
- from_plan: single-key ct0 patch
  actual: pair write
  reason: keeps the pair consistent; X_AUTH_TOKEN_SAVED_AT now means last written
- from_plan: implementer redirected process stdout around the sink write
  actual: host switched to BaoSink(echo=None) from ri-14
  reason: redirect_stdout swaps stdout for the whole process
recommendations:
- 'ri-12: stop_when=lambda ids: all(i in known for i in ids); collect all pages before
  persisting; map rate_limited and page_cap to a partial warning; reuse tests/fixtures/x_bookmarks_graphql.py'
- 'ri-13: use XPost.outbound_urls plus quoted.outbound_urls'
- 'Not exercised against live x.com: chunk-map pattern, query-not-found wording, session
  error codes and the discovery login check are unverified'
---

# Learning: ri-11

## Decisions
- **XBookmarksClient.iter_pages**: newest-first pages by cursor-bottom, stop_when(page_ids) hook, page cap, x_bookmarks_page_delay_s pacing; WalkStopReason exhausted/stop_when/page_cap/rate_limited/caller_stopped
- **Durable query ID cache**: settings_overrides key x_bookmarks.graphql_query_id; stale answer clears, rediscovers once, retries once; bundle requests never carry cookies
- **ct0 write-back as a pair**: X_AUTH_TOKEN and X_CT0 in one quiet BaoSink.write() on the worker's authenticated client, then apply_local_write with the pinned saved_at; failure never fails the fetch
- **Value-free XPost records**: note-tweet full text, outbound URLs without X self links, best mp4, UTC created_at, one level of quoted post

## Deviations
- Plan: single-key ct0 patch → Actual: pair write (reason: keeps the pair consistent; X_AUTH_TOKEN_SAVED_AT now means last written)
- Plan: implementer redirected process stdout around the sink write → Actual: host switched to BaoSink(echo=None) from ri-14 (reason: redirect_stdout swaps stdout for the whole process)

## Recommendations
- ri-12: stop_when=lambda ids: all(i in known for i in ids); collect all pages before persisting; map rate_limited and page_cap to a partial warning; reuse tests/fixtures/x_bookmarks_graphql.py
- ri-13: use XPost.outbound_urls plus quoted.outbound_urls
- Not exercised against live x.com: chunk-map pattern, query-not-found wording, session error codes and the discovery login check are unverified
