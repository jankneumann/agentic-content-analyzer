<!-- SCAFFOLD: generated by plan-roadmap from roadmap `x-bookmarks-session-capture`, item `ri-09`.
     Preliminary and intended for refinement by /plan-feature or /iterate-on-plan
     before implementation. The capability directory (`x-bookmarks-session-capture`) is a
     placeholder unless the item named one explicitly. -->

## ADDED Requirements

### Requirement: src/contracts/workflow_models.py contains XBookmarksIngestCommand and...

The system SHALL ensure that src/contracts/workflow_models.py contains XBookmarksIngestCommand and matches a fresh regeneration from openapi/v1.yaml with no hand edits.

#### Scenario: src/contracts/workflow_models.py contains XBookmarksIngestCommand and...

WHEN `add-x-bookmarks-contract-and-content-source` is implemented
THEN src/contracts/workflow_models.py contains XBookmarksIngestCommand and matches a fresh regeneration from openapi/v1.yaml with no hand edits.

### Requirement: alembic heads reports a single head and the migration uses ALTER TYPE...

The system SHALL ensure that alembic heads reports a single head and the migration uses ALTER TYPE contentsource ADD VALUE IF NOT EXISTS so it is idempotent.

#### Scenario: alembic heads reports a single head and the migration uses ALTER TYPE...

WHEN `add-x-bookmarks-contract-and-content-source` is implemented
THEN alembic heads reports a single head and the migration uses ALTER TYPE contentsource ADD VALUE IF NOT EXISTS so it is idempotent.

### Requirement: Contract tests accept ingest.x-bookmarks and x_bookmarks as valid literals

The system SHALL ensure that contract tests accept ingest.x-bookmarks and x_bookmarks as valid literals.

#### Scenario: Contract tests accept ingest.x-bookmarks and x_bookmarks as valid literals

WHEN `add-x-bookmarks-contract-and-content-source` is implemented
THEN Contract tests accept ingest.x-bookmarks and x_bookmarks as valid literals.
