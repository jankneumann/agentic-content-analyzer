# Contracts

This closeout is expected to reconcile the source override paths in
`openspec/contracts/content-workflows/openapi/v1.yaml` with the existing server
and generated clients. Contract drift must remain zero.
