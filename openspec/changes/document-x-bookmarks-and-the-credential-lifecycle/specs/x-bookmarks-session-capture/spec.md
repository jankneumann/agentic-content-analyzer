<!-- SCAFFOLD: generated by plan-roadmap from roadmap `x-bookmarks-session-capture`, item `ri-17`.
     Preliminary and intended for refinement by /plan-feature or /iterate-on-plan
     before implementation. The capability directory (`x-bookmarks-session-capture`) is a
     placeholder unless the item named one explicitly. -->

## ADDED Requirements

### Requirement: Every new CLI command and setting appears in the CLAUDE.md documentation...

The system SHALL ensure that every new CLI command and setting appears in the CLAUDE.md documentation index tables and docs/SETUP.md.

#### Scenario: Every new CLI command and setting appears in the CLAUDE.md documentation...

WHEN `document-x-bookmarks-and-the-credential-lifecycle` is implemented
THEN Every new CLI command and setting appears in the CLAUDE.md documentation index tables and docs/SETUP.md.

### Requirement: docs/MOBILE_CAPTURE.md states that bookmarking on X replaces the share-sheet...

The system SHALL ensure that docs/MOBILE_CAPTURE.md states that bookmarking on X replaces the share-sheet path for X content.

#### Scenario: docs/MOBILE_CAPTURE.md states that bookmarking on X replaces the share-sheet...

WHEN `document-x-bookmarks-and-the-credential-lifecycle` is implemented
THEN docs/MOBILE_CAPTURE.md states that bookmarking on X replaces the share-sheet path for X content.

### Requirement: docs/GOTCHAS.md contains entries for frozen settings versus the live...

The system SHALL ensure that docs/GOTCHAS.md contains entries for frozen settings versus the live provider and for KV v2 create_or_update clobbering sibling keys.

#### Scenario: docs/GOTCHAS.md contains entries for frozen settings versus the live...

WHEN `document-x-bookmarks-and-the-credential-lifecycle` is implemented
THEN docs/GOTCHAS.md contains entries for frozen settings versus the live provider and for KV v2 create_or_update clobbering sibling keys.
