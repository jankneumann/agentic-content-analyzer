# Change Context: stuck-content-sweeper-and-requeue-cli

## Requirement Traceability Matrix

| Req ID | Spec Source | Description | Contract Ref | Design Decision | Files Changed | Test(s) | Evidence |
|--------|------------|-------------|-------------|----------------|---------------|---------|----------|
| content-state-reconciliation.1 | `specs/content-state-reconciliation/spec.md` — Scanning and reports are strictly bounded | One page is limited to 1..100 candidates and reports expose only a closed, non-sensitive projection with keyset continuation. The exact scan remains bounded over 10,001 irrelevant rows. | report schema | D10 | contracts; `src/services/content_reconciliation_service.py`; `tests/integration/test_content_reconciliation_query_plan.py` | contract bounds; `test_dry_run_is_read_only_bounded_and_keyset_paginated`; `test_candidate_scan_uses_bounded_existing_index` | GREEN: existing `ix_contents_status` scanned 1 Content row; no additional index justified |
| content-state-reconciliation.2 | `specs/content-state-reconciliation/spec.md` — Dry-run is reconciliation-read-only | Dry-run uses a SELECT-only path, produces proposed projections, and leaves Content, jobs, and action audit unchanged. | request/report schema | D8 | contracts; `src/services/content_reconciliation_service.py` | `test_dry_run_is_read_only_bounded_and_keyset_paginated`, `test_dry_run_rejects_multi_content_url_success_evidence` | GREEN: before/after state snapshots identical and audit count zero |
| content-state-reconciliation.3 | `specs/content-state-reconciliation/spec.md` — Apply action evidence is atomic | Applied items read back actual after-values; each mutation, audit insert, and retry notification shares one supplied connection and per-item savepoint. | item/audit schemas | D6, D8, D9 | contracts; `src/services/content_reconciliation_service.py` | `test_apply_projects_matching_summary_and_audits_on_supplied_connection`, audit rollback/continuation, notification commit/rollback tests | GREEN: audit failure rolls back mutation and notification while later page items continue |
| content-state-reconciliation.4 | `specs/content-state-reconciliation/spec.md` — Canonical remote controls expose reconciliation | The authenticated operation API exposes a synchronous bounded `POST /api/v1/operations/reconcile-content` contract with 200/401/403/422/503 responses. | `openspec/contracts/content-workflows/openapi/v1.yaml#/paths/~1api~1v1~1operations~1reconcile-content` | D11 | canonical contract; `src/api/operation_routes.py`; `src/api/workflow_dependencies.py`; `src/clients/workflow_api_client.py`; `src/cli/workflow_commands.py`; `evaluation/descriptors/aca-cli.yaml`; reconciliation gen-eval scenarios | contract semantics; authenticated audited preview; disabled apply; one-page client transport; CLI dry-run/apply/continuation/safe JSON/exit-policy tests; checked-in preview/disabled/guarded/failure cases | GREEN: exact package gate and 192-test CLI gen-eval suite passed; one request-scoped queue connection is supplied to the settings-configured service; CLI never traverses automatically and exits nonzero only for request failure or `apply_failed` apply items |
| content-state-reconciliation.5 | `specs/content-state-reconciliation/spec.md` — Retry is canonical and atomically budgeted | Reconciliation transitions stale Content to failed with an owner-version advance, then calls the canonical connection-scoped retry primitive with the configured ceiling. | --- | D6, D7 | settings; `src/services/content_reconciliation_service.py`; `src/services/operation_service.py` | `test_apply_recovers_stale_owner_to_failed_then_retries_same_operation`, notification/audit tests | GREEN: parsing and processing retry paths observe queued/1 and protocol reset |
| content-state-reconciliation.6 | `specs/content-state-reconciliation/spec.md` — Stale apply is locked and protocol-gated | Apply defaults off; mutating proposals take the content try-lock then graph/root/target/Content/Summary locks, revalidate evidence, and reject legacy protocol. Pure no-ops do not take lifecycle locks. | --- | D5-D8, D11 | settings; `src/services/content_reconciliation_service.py` | disabled apply; revalidation; contention; cancellation; force/protocol; vanished-graph tests | GREEN: real PostgreSQL lock, refresh, no-op, and fail-closed races pass |
| content-state-reconciliation.7 | `specs/content-state-reconciliation/spec.md` — Supported transitions persist exact ownership | Content persists a complete operation/generation/phase/version token; guarded writers acquire and renew the exact token, while unsupported status writers clear unchanged tokens. | `contracts/db/schema.sql#contents` | D1, D5, D12 | migration; `src/models/content.py`; `src/queue/execution_claim.py`; `src/queue/content_execution_lock.py`; guarded URL and Summary writers | `test_same_operation_new_generation_renews_failed_parsing_owner`, `test_content_transaction_lock_excludes_second_connection`, `test_unsupported_status_writer_clears_unchanged_owner_token`, `test_supported_status_writer_that_advances_version_keeps_new_owner` | GREEN: exact PostgreSQL concurrency gate (11 passed) plus ORM parity gate |
| content-state-reconciliation.8 | `specs/content-state-reconciliation/spec.md` — Claim generations fence every attempt | Queue claims advance generation in the database and every nonqueued-to-queued transition resets protocol to legacy-safe version 1. | `contracts/db/schema.sql#pgqueuer_jobs` | D13 | migration; `src/models/jobs.py`; `src/queue/setup.py` | `test_deployed_schema_fences_claims_and_legacy_status_writers`, `test_bootstrap_installs_claim_defaults_and_triggers`, `test_job_record_preserves_legacy_projection_defaults` | GREEN: legacy and reclaimed claims passed |
| content-state-reconciliation.9 | `specs/content-state-reconciliation/spec.md` — Domain projection requires matching provenance | Summary provenance stores the exact operation/generation pair, and final guarded writes atomically insert the Summary and complete Content only for the current claim. | `contracts/db/schema.sql#summaries` | D3, D5, D12 | migration; `src/models/summary.py`; `src/processors/summarizer.py`; `src/queue/execution_claim.py` | `test_summary_commit_records_exact_provenance`, `test_summary_superseded_during_compute_inserts_nothing` | GREEN: exact PostgreSQL concurrency gate proves provenance and late-commit fencing |
| content-state-reconciliation.10 | `specs/content-state-reconciliation/spec.md` — Apply action evidence is atomic | Applied action evidence is append-only, stores observed status/retry projections, and commits atomically with the mutation and notification. | audit schema | D9, D12 | migration; model; `src/services/content_reconciliation_service.py` | schema constraints; `test_retry_notification_commits_with_action_audit`; `test_retry_notification_rolls_back_when_audit_insert_fails` | GREEN: commit and rollback boundaries both exercised |
| job-management.1 | `specs/job-management/spec.md` — Queue claims carry fencing generations | Current claims carry a task-local generation; queue and domain writes revalidate that generation and cancellation, returning typed cancelled/superseded outcomes without stale failure writes. | `contracts/db/schema.sql#pgqueuer_jobs` | D2, D4, D13 | `src/queue/execution_claim.py`; `src/queue/worker.py`; `src/queue/setup.py`; `src/services/operation_service.py` | lifecycle fence tests; `test_process_job_treats_claim_cancelled_as_expected_outcome`, `test_process_job_drops_superseded_domain_outcome`, concurrency cancellation/supersession tests | GREEN: exact writer/worker gate plus real PostgreSQL cancellation and reclaim races |
| agentic-operations.2 | `specs/agentic-operations/spec.md` — Canonical URL retry resumes exact Content | A strict partial extraction checkpoint, or one unambiguous persisted owner fallback, resumes the exact URL Content directly without reclassification or aggregate deduplication. | `IngestionResultV2` | D1, D2, D4 | `src/ingestion/orchestrator.py`; `src/queue/workflow_handlers.py`; `src/services/url_extractor.py`; `tests/integration/test_content_reconciliation_url_resume.py` | URL resume checkpoint validation/fallback tests; handler direct-resume tests; `test_canonical_url_retry_resumes_exact_owned_content`; guarded extractor success/failure tests | GREEN: two real-PostgreSQL end-to-end cases prove public retry, generation-2 direct resume, process-loss owner fallback, stale-error/owner clearing, result replacement, and no duplicate Content |
| agentic-operations.1 | `specs/agentic-operations/spec.md` — Internal retry supports an optional atomic ceiling | Public retry reuses one connection-scoped primitive after graph/root locks; the primitive verifies lock ownership, locks the target, applies an optional atomic ceiling, preserves only the closed exact-content URL checkpoint, and notifies transactionally. | `contracts/db/schema.sql#pgqueuer_jobs` | D6, D7 | `src/services/operation_service.py`; `tests/integration/test_content_reconciliation_url_resume.py` | locked retry unit/profile tests; two-caller ceiling race; canonical URL end-to-end retry | GREEN: public retry preserves strict checkpoint evidence, increments once below the configured ceiling, and feeds the newer canonical claim |

## Design Decision Trace

| Decision | Rationale | Implementation | Why This Approach |
|----------|-----------|----------------|-------------------|
| D7 | Bound stale detection, batch work, lock waits, statements, and retry attempts. | Validated `Settings` fields with documented safe defaults and one timeout-order validator. | Direct Pydantic constraints keep policy validation at process startup and avoid a second configuration layer. |
| D2 | Fence every queue attempt and keep old-worker behavior safe. | A shared `ContextVar` carries the exact claim generation; worker, setup, and operation lifecycle mutations require `(id, in_progress, generation)`, while the database trigger resets every requeue to protocol 1. | Task-local propagation reaches async handlers and `to_thread` work without expanding every handler signature, and guarded updates fail closed after claim loss. |
| D1 | Persist transition ownership with the status write. | Guarded acquisition atomically writes operation, generation, phase, and an advancing owner version; an ORM hook mirrors trigger clearing for unsupported writers. | Exact persisted ownership supports safe renewal and recovery without inferring ownership from payload chronology. |
| D3 | Scope domain completion evidence to one attempt. | URL success clears its exact owner and stale error; Summary completion stores operation/generation provenance in the same guarded transaction as Content completion. | Old extraction or Summary output cannot authorize repair or completion for a newer attempt. |
| D4 | Give cancellation and force precedence. | Domain guards return `ClaimCancelled` or `ClaimSuperseded`; workers checkpoint cancellation and drop superseded work without generic failure writes. | Typed outcomes prevent stale computations from overwriting the current lifecycle result. |
| D5 | Serialize transactions and fence every write. | Supported domain transactions take a namespaced PostgreSQL advisory transaction lock, then lock/revalidate job and Content before mutation. | Computation remains connection-free while its final commit is serialized and rejected after reclaim. |
| D6 | Preserve graph-wide lock order across retry and reconciliation. | Public retry takes graph then root locks; the private connection-scoped primitive verifies that graph lock, takes the target row, mutates on the supplied connection, and sends notification in the same transaction. | A single mutation primitive avoids nested connections and keeps root-to-target ordering observable and testable. |
| D8 | Keep preview persistence-free and distinguish proposed from observed results. | The service has a SELECT-only dry-run path and a separate per-item savepoint apply path. | Preview cannot emit notifications, audit rows, or rolled-back side effects. |
| D9 | Make every applied mutation externally auditable without leaking payload data. | Apply reads actual after-state and inserts one closed action row in the same savepoint; failure becomes `apply_failed` and processing continues. | Audit and report agree with committed state, while item isolation preserves bounded progress. |
| D10 | Ensure deterministic bounded scans and safe continuation. | The service uses ascending Content ID keysets and `limit+1`; a 10,001-row plan test measured one Content row via the existing status index. | Measured evidence keeps scans bounded without adding redundant candidate/provenance indexes. |
| D11 | Fail closed during rollout. | Apply defaults off in settings and request schema; the endpoint declares a 503 problem response. | A default-off server gate preserves existing behavior and gives rollback a single switch. |
| D12 | Persist ownership and action evidence without coupling retention lifecycles. | Add nullable complete owner/provenance tokens plus an append-only action table that retains IDs without foreign keys. | Legacy writes remain valid, while reconciliation can prove exact ownership and historical evidence survives queue/content retention. |
| D13 | Preserve old-worker safety while introducing claim fencing. | Database triggers increment every queue claim generation and reset every requeue to protocol 1; current workers opt into protocol 2 later. | Trigger-driven defaults cover workers that do not know the new columns and make rollback behavior explicit. |

## Review Findings Summary

| Finding ID | Package | Type | Criticality | Disposition | Resolution |
|------------|---------|------|-------------|-------------|------------|
| IR-001 | independent review | concurrency | High | resolved | Locked SQLAlchemy Content reads now use `populate_existing=True`; two displacement-during-compute regressions prove cached ownership cannot authorize a late commit. |
| IR-002 | independent review | consistency | Medium | accepted | Apply no-op decisions intentionally return the bounded scan snapshot and take no lifecycle locks, matching the explicit fresh-active/no-op contract. |
| IR-003 | independent review | observability | Medium/Low | deferred | Provider-error redaction and structured per-item apply-failure logging are tracked in GitHub issue #484. |

## Coverage Summary

- **Requirements traced**: 13/13 implemented package requirements
- **Tests mapped**: 13 requirements have at least one test
- **Evidence collected**: Domain fencing has direct real-PostgreSQL concurrency evidence; checked-in CLI gen-eval covers preview and guarded failure modes; canonical URL checkpoint and owner-fallback recovery pass end to end
- **Gaps identified**: No Critical or High findings remain; observability hardening is tracked in GitHub issue #484
- **Deferred items**: No candidate/provenance index was added because the required 10,001-row plan measured one Content row using the existing status index

## Final Validation Evidence

- Exact cumulative gate: `.venv/bin/pytest tests/ --no-cov -q` selected 6,146 tests and exited 0.
- Canonical surfaces: 111 API/client/CLI tests passed.
- CLI gen-eval: 192 tests passed, including four checked-in reconciliation scenarios.
- Reconciliation core: 23 service/query-plan tests, 11 concurrency tests, and 2 URL-recovery E2E tests passed.
- Contract and schema gates: strict OpenSpec validation, workflow contract drift, Ruff, and the single Alembic head `8a5c3e7f9b21` passed.

## Operator and Canonical Spec Reconciliation

- `docs/DEVELOPMENT.md` documents dry-run-first use, explicit one-page continuation,
  the default-off apply gate, exit policy, safe report fields, no-op decisions, and
  bounded settings.
- `docs/ARCHITECTURE.md` records claim-generation fencing, exact URL resume,
  reconciliation lock/transaction ordering, and the canonical endpoint.
- Canonical `agentic-operations`, `job-management`, and `cli-interface` specs now
  contain the implemented retry-ceiling, claim/content fencing, and remote bounded
  CLI requirements. The new `content-state-reconciliation` capability remains a
  change delta until the roadmap's independent final review and archive/sync step.
