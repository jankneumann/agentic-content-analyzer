# Contracts

No runtime contract change is assumed. The refined plan must use the existing
search metadata and telemetry boundaries or explicitly propose a contract
delta if production evidence cannot be obtained without one.
