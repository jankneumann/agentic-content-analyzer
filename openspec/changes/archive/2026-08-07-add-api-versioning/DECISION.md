# RI-11 disposition: defer and archive

Decision: do not implement this proposal's 42 tasks. No concrete incompatible
HTTP contract or independently deployed consumer currently requires two live URL
versions.

The durable decision, evidence, and explicit reconsideration triggers are recorded
in `docs/decisions/0002-defer-api-versioning-until-an-incompatible-boundary.md`.
This change is archived as speculative history, and its delta spec is deliberately
not synchronized into `openspec/specs/`.
