# Reconciliation: ingestion filtering and prioritization

**Disposition**: Archive the implemented historical foundation with unsupported
tasks reopened. Continue remaining work in
`reconcile-ingestion-filtering-runtime-contract`.

## Verified foundation

- Models, migrations, global configuration, filter service, tier primitives,
  persisted non-dry-run decisions, post-ingest hook, and basic filter commands.
- RI-03 focused gate: filtering/source-override group passed 68 tests.

## Reopened historical claims

Task boxes were reopened where current evidence does not satisfy the original
language-gate, per-source override, exact span, canonical CLI flag, full
explain/rerun, content projection, feedback wiring, regression, or
documentation promise.

Only the evidenced foundation was published to
`openspec/specs/ingestion-filtering/spec.md`. The original delta is historical
and SHALL NOT be synchronized automatically.
